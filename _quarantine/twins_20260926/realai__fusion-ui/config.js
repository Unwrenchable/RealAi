/* Generated to match repo-root `.env` — keep in sync with REALAI_API_BASE / PORT / REALAI_API_KEY.
 * When Fusion UI is served by `python -m realai.api_server`, `/fusion-ui/config.js` is emitted
 * live from server settings and overrides this file.
 */
window.__REALAI_CONFIG__ = {
  BACKEND_BASE: 'http://127.0.0.1:8001',
  API_KEY: 'local',
  PORT: 8001,
};
