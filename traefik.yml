# HTTP-only Traefik for local docker (TLS disabled — empty certs broke startup)
entryPoints:
  web:
    address: ":80"

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false

api:
  insecure: true
  dashboard: true
