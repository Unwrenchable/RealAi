{
  "name": "@realai/cli",
  "version": "0.1.0",
  "private": true,
  "description": "RealAI CLI — command-line interface for the local Hive",
  "type": "module",
  "bin": {
    "realai": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "ora": "^8.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
