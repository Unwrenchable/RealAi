# Fusion UI — legacy status (2026-09-08)

`fusion-ui` is **legacy HTML**. It is intentionally kept for static asset serving.

- App shell UX for fusion is redirected to `/console?fusion=1` on the hive orchestrator (:8001).
- Do not delete this tree; Next.js App Router at product-root `frontend\` is the canonical interactive UI.
- Soft-collapse only: assets stay; no hard delete.

Park / collapse notes: `C:\RealAI-clean\_quarantine\_parked\2026-09-08-ui-collapse\COLLAPSE_REPORT.md`
