# Static reconstruction — do not boot

This tree was restored without starting:

- servers / orchestrator routes
- training
- self_improve
- dispatch
- HTTP / RPC
- RealAI runtime, agents, or benchmarks

Recovered scanner `main()` entry points still exist in copied source.
Do not run them until you review.
