# Training data (promoted Phase 2)

- `realai_finetune_dataset.jsonl` â€” instruction/response samples
- `agent_manifests_for_finetuning.json` â€” agent roles for finetune

Imported from Downloads. Use with `realai/training` / self_improvement pipeline.
