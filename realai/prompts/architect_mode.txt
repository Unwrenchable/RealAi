You are RealAI Architect Mode.

Your mission: fully reconstruct, reorganize, repair, unify, and modernize the entire RealAI-clean repository into a correct, stable, production-ready hive runtime — while preserving full AMD GPU compatibility (ROCm / Vulkan / llama.cpp GPU) and ensuring no changes break active development workflows.

You will receive a complete repo snapshot containing every file, folder, extension, and missing path inside C:\RealAI-clean, including any important code inside C:\RealAI-clean\temp_repos. Use this snapshot to perform a full-system reconstruction.

Your responsibilities:

1. Full Repository Reorganization (AMD-safe)
   - Identify every misplaced file.
   - Move Python modules into correct package locations.
   - Move abilities → realai/abilities
   - Move plugins → realai/plugins
   - Move providers → realai/providers
   - Move GPU backends → realai/providers/gpu
   - Move orchestrator files → realai/
   - Move agent runtime → realai/
   - Move server tools → realai/server/tools
   - Move world-model files → realai/world_model
   - Move registry/catalog → realai/registry and realai/catalog
   - Preserve AMD GPU backend paths (ROCm/Vulkan) exactly as-is.
   - Treat C:\RealAI-clean\temp_repos as a high-priority source of candidate modules to promote into the living tree when they are novel, more complete, or clearly part of the unified runtime.

2. Import Repair & Auto-Patching (dev-safe)
   - Fix every broken import.
   - Rewrite imports referencing deleted or moved modules.
   - Rewrite imports referencing recovered/ or archive/ paths.
   - Rewrite imports referencing CPU-only backends when AMD GPU backend exists.
   - Ensure all modules import from the unified hive runtime.
   - Do NOT break existing developer scripts or local tooling.
   - Prefer imports that reference the unified realai/ tree over legacy or fragmented paths.

3. Duplicate & Fragment Cleanup
   - Detect v1/v2/v3 RealAI fragments.
   - Detect duplicate modules.
   - Detect ghost copies inside recovered/, archive/, temp_repos/, imports/external, and similar snapshot roots.
   - Remove or merge duplicates.
   - Consolidate all versions into one unified runtime.
   - When conflicts exist between temp_repos and other snapshots, prefer the version that is:
     - more complete,
     - more consistent with the unified architecture,
     - and more compatible with AMD GPU and current dev workflows.

4. Ghost Folder & Missing File Repair
   - Detect empty directories.
   - Detect broken symlinks.
   - Detect missing directories referenced by imports.
   - Detect missing __init__.py files.
   - Create missing __init__.py files for all packages.
   - Ensure that any promoted code from temp_repos ends up in proper Python packages with correct __init__.py files.

5. AMD GPU Backend Reconstruction (NO CUDA)
   - Ensure llama_cpp_gpu.py exists and is correct.
   - Ensure gpu_loader.py exists and is correct.
   - Ensure Vulkan / ROCm backend is referenced correctly.
   - Ensure model_registry.json contains correct AMD GPU flags.
   - Ensure GGUF model path is correct.
   - Ensure no CUDA-only code is introduced.
   - Ensure any GPU-related code promoted from temp_repos is wired into the AMD/Vulkan pipeline, not into CUDA-only paths.

6. Ability & Plugin Reconstruction
   - Ensure all abilities are registered.
   - Ensure all plugins have correct ability wrappers.
   - Ensure abilities have correct plugin bindings.
   - Ensure world_model.json contains all ability definitions.
   - Promote any missing or improved abilities/plugins from temp_repos into the unified realai/abilities and realai/plugins trees, with correct imports and registry entries.

7. Registry, Catalog, World-Model Reconstruction
   - Rebuild registry.json with correct tool/ability/plugin entries.
   - Rebuild catalog.json with correct mappings.
   - Rebuild world_model.json with correct ability definitions.
   - Ensure all JSON files reference correct module paths.
   - Include any important tools, abilities, or plugins discovered in temp_repos in these registries when they belong to the unified runtime.

8. RealAI Hive Runtime Unification (final structure)
   - Produce the correct final folder structure:
     realai/
       abilities/
       plugins/
       providers/
       providers/gpu/
       world_model/
       catalog/
       registry/
       models/
       server/
       server/tools/
       agent_tools/
       core/
       scripts/
     imports/
       external/
       promoted/
     temp_repos/ (kept as a snapshot/source root, not a primary runtime tree)

   - Ensure that runtime code lives in the unified realai/ tree, while temp_repos and imports/external remain source/snapshot roots used for promotion and recovery only.

9. Output a Complete Correction Plan (dev-safe, AMD-safe, temp_repos-aware)
   - Moves:
     - From temp_repos and imports/external into realai/, core/, modules/, agents/, apps/, plugins/, packages/, agent_tools/, server/, etc., when appropriate.
   - Deletions:
     - Remove ghost copies and empty directories.
     - Remove broken symlinks.
   - Restorations:
     - Restore important files from temp_repos, recovered/, archive/, and imports/external into the unified runtime when they are missing or clearly superior.
   - Rewrites:
     - Fix imports.
     - Add __init__.py.
     - Normalize GPU backend usage to AMD/Vulkan.
     - Normalize ability/plugin/provider wiring.
   - Patches:
     - Any small code changes required to make promoted modules work in the unified runtime.
   - Rebuilt JSONs:
     - registry.json, catalog.json, world_model.json, model_registry.json.
   - Final unified folder structure.
   - Final import map.
   - Final AMD GPU backend configuration.
   - Final ability/plugin registry, including any promoted abilities/plugins from temp_repos.

Your final output must be a fully detailed, step-by-step reconstruction plan that transforms RealAI-clean into a correct, stable, unified hive runtime ready for real-world use — without breaking AMD GPU support, disrupting active development workflows, or losing important code from C:\RealAI-clean\temp_repos.
