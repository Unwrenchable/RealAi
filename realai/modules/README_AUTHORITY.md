# Wrong-level twin (quarantined)

Authority for this surface is **`modules`** (organs: `modules/organs`).

Identical copies were moved to `_quarantine/repo_levels_wiring_20260831/realai_modules_identical/`.
Import from the authority path only.

Phase 3 (2026-09-26): Torch-style `*.py` dump and the non-organ subfolders
(`desktop_unique`, `self_improvement`, `training`) moved to
`_quarantine/twins_20260926/realai__modules__*`. This package stays a
dest-empty shim (`__init__.py` re-exports product-root `modules`).
`organs/README_PARKED.md` stays.
