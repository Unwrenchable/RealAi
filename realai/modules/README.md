# Living promoted modules (non-core packages)

Authority is product-root `modules/` (`modules/organs`). This folder is the
compat shim. The Torch `nn` dump that used to sit beside `__init__.py` is in
`_quarantine/twins_20260926/realai__modules__torch_nn`.

