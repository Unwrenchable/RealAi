# effective-engine (`effective-engine-main-learned`)

Learned RealAI module stub — same provider boundary as
`rackup-coach` / `atomicfizz-coach`. Generated by `python -m realai.learn_git`.

| | |
|--|--|
| Plugin id | `effective-engine-main-learned` |
| Package | `plugins.effective_engine_main_learned` |
| Folder | `realai/plugins/effective_engine_main_learned` |
| Version | `0.1.0` (learned module) |

This is **not** a runtime heal. Abilities return structured placeholders.

## Abilities (placeholders)

| Ability | Description |
|---------|-------------|
| `health` | Stub liveness / contract discovery (no runtime heal) |
| `repo_map` | Summarize learned tree layout and stack |
| `node_context` | Placeholder context hints for node (product owns runtime) |
| `fastify_context` | Placeholder context hints for fastify (product owns runtime) |
| `agents_bridge` | Bridge stub for existing .github/agents/ surface |

Canonical HTTP: `POST {REALAI_BASE_URL}/v1/plugins/effective-engine-main-learned`
