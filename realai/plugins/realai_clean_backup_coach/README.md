# RealAi (`realai-clean-backup-coach`)

Learned **living** RealAI plugin stub — same provider boundary as
`rackup-coach` / `atomicfizz-coach`. Generated by `python -m realai.learn_git`.

| | |
|--|--|
| Plugin id | `realai-clean-backup-coach` |
| Package | `plugins.realai_clean_backup_coach` |
| Folder | `realai/plugins/realai_clean_backup_coach` |
| Version | `0.1.0` (learned stub) |

This is **not** a runtime heal. Abilities return structured placeholders.

## Abilities (placeholders)

| Ability | Description |
|---------|-------------|
| `health` | Stub liveness / contract discovery (no runtime heal) |
| `repo_map` | Summarize learned tree layout and stack |
| `node_context` | Placeholder context hints for node (product owns runtime) |
| `pnpm_context` | Placeholder context hints for pnpm (product owns runtime) |
| `python_context` | Placeholder context hints for python (product owns runtime) |
| `docker_context` | Placeholder context hints for docker (product owns runtime) |
| `typescript_context` | Placeholder context hints for typescript (product owns runtime) |
| `abilities_bridge` | Bridge stub for existing abilities/ surface |
| `adapters_bridge` | Bridge stub for existing adapters/ surface |
| `agents_bridge` | Bridge stub for existing agents/ surface |
| `modules_bridge` | Bridge stub for existing modules/ surface |
| `packages_bridge` | Bridge stub for existing packages/ surface |
| `plugins_bridge` | Bridge stub for existing plugins/ surface |
| `route_v1_audio_transcriptions` | Hint stub for discovered route /v1/audio/transcriptions |
| `route_v1_audio_speech` | Hint stub for discovered route /v1/audio/speech |
| `route_v1_chat_completions` | Hint stub for discovered route /v1/chat/completions |

Canonical HTTP: `POST {REALAI_BASE_URL}/v1/plugins/realai-clean-backup-coach`
