# Plugins authority

**Live:** `realai/plugins` (`import realai.plugins`)

Identical root twin files were moved to `_quarantine/plugins_twin_20260831/`.
This package is a compat shim that re-exports `realai.plugins`.
