[
  {
    "id": "proposal_20260601_092636_974010",
    "created_at": "2026-06-01T09:26:36.974052Z",
    "module": "C:\\Users\\tsmit\\realai\\realai\\plugins\\knowledge_rag.py",
    "objective": "Tighten error handling without breaking behavior.",
    "diff": "--- C:\\Users\\tsmit\\realai\\realai\\plugins\\knowledge_rag.py\n+++ C:\\Users\\tsmit\\realai\\realai\\plugins\\knowledge_rag.py (proposed)\n@@ -1,120 +1 @@\n-import json\n-import re\n-from pathlib import Path\n-from typing import Any, Dict, List\n-\n-from realai.plugins.llm_chat import LLMChatPlugin\n-\n-\n-class KnowledgeRAGPlugin:\n-    name = \"knowledge_rag\"\n-\n-    def __init__(self, store_path: str = \"realai_knowledge_store.json\"):\n-        self.store_path = Path(store_path)\n-        self._llm = LLMChatPlugin()\n-        self._docs = self._load_store()\n-\n-    def _load_store(self) -> List[Dict[str, Any]]:\n-        if not self.store_path.exists():\n-            return []\n-        try:\n-            return json.loads(self.store_path.read_text(encoding=\"utf-8\"))\n-        except Exception:\n-            return []\n-\n-    def _save_store(self) -> None:\n-        self.store_path.write_text(json.dumps(self._docs, indent=2), encoding=\"utf-8\")\n-\n-    def ingest_text(self, title: str, content: str, source: str = \"manual\") -> Dict[str, Any]:\n-        doc_id = \"doc_{0}\".format(len(self._docs) + 1)\n-        self._docs.append(\n-            {\n-                \"id\": doc_id,\n-                \"title\": title,\n-                \"content\": content,\n-                \"source\": source,\n-            }\n-        )\n-        self._save_store()\n-        return {\"status\": \"success\", \"id\": doc_id}\n-\n-    def ingest_file(self, file_path: str) -> Dict[str, Any]:\n-        path = Path(file_path)\n-        if not path.exists():\n-            return {\"status\": \"failed\", \"error\": \"File not found: {0}\".format(file_path)}\n-\n-        suffix = path.suffix.lower()\n-        if suffix in (\".md\", \".txt\", \".py\", \".json\", \".toml\", \".yaml\", \".yml\"):\n-            content = path.read_text(encoding=\"utf-8\", errors=\"ignore\")\n-            return self.ingest_text(title=path.name, content=content, source=str(path))\n-\n-        if suffix == \".pdf\":\n-            try:\n-                from pypdf import PdfReader  # type: ignore\n-\n-                reader = PdfReader(str(path))\n-                parts = []\n-                for page in reader.pages:\n-                    parts.append(page.extract_text() or \"\")\n-                content = \"\\n\".join(parts)\n-                return self.ingest_text(title=path.name, content=content, source=str(path))\n-            except Exception as exc:\n-                return {\"status\": \"failed\", \"error\": \"PDF parsing failed: {0}\".format(exc)}\n-\n-        return {\"status\": \"failed\", \"error\": \"Unsupported file type: {0}\".format(suffix)}\n-\n-    def _score(self, query: str, content: str) -> float:\n-        query_terms = set(re.findall(r\"[a-zA-Z0-9_]+\", query.lower()))\n-        content_terms = set(re.findall(r\"[a-zA-Z0-9_]+\", content.lower()))\n-        if not query_terms:\n-            return 0.0\n-        overlap = len(query_terms.intersection(content_terms))\n-        return overlap / float(len(query_terms))\n-\n-    def retrieve(self, query: str, top_k: int = 3) -> List[Dict[str, Any]]:\n-        scored = []\n-        for doc in self._docs:\n-            score = self._score(query, doc.get(\"content\", \"\"))\n-            if score > 0:\n-                scored.append((score, doc))\n-        scored.sort(key=lambda item: item[0], reverse=True)\n-\n-        results = []\n-        for score, doc in scored[:top_k]:\n-            results.append(\n-                {\n-                    \"id\": doc.get(\"id\"),\n-                    \"title\": doc.get(\"title\"),\n-                    \"source\": doc.get(\"source\"),\n-                    \"score\": round(score, 4),\n-                    \"content_preview\": doc.get(\"content\", \"\")[:1000],\n-                }\n-            )\n-        return results\n-\n-    def query(self, question: str, top_k: int = 3) -> Dict[str, Any]:\n-        retrieved = self.retrieve(question, top_k=top_k)\n-        if not retrieved:\n-            return {\n-                \"status\": \"success\",\n-                \"question\": question,\n-                \"answer\": \"No relevant knowledge found in local store.\",\n-                \"sources\": [],\n-            }\n-\n-        context = \"\\n\\n\".join(\n-            \"[{0}] {1}\\n{2}\".format(item[\"id\"], item[\"title\"], item[\"content_preview\"])\n-            for item in retrieved\n-        )\n-        prompt = (\n-            \"Answer the question using only the provided context. Cite source ids in brackets.\\n\"\n-            \"Question: {0}\\n\\nContext:\\n{1}\"\n-        ).format(question, context)\n-        generated = self._llm.generate(prompt=prompt, temperature=0.2, max_tokens=600)\n-        return {\n-            \"status\": \"success\",\n-            \"question\": question,\n-            \"answer\": generated.get(\"content\", \"\"),\n-            \"sources\": retrieved,\n-            \"provider\": generated.get(\"provider\"),\n-        }\n+[RealAI] No LLM backend available. Install ollama or set GROQ_API_KEY / OPENROUTER_API_KEY / XAI_API_KEY.",
    "proposed_content": "[RealAI] No LLM backend available. Install ollama or set GROQ_API_KEY / OPENROUTER_API_KEY / XAI_API_KEY.",
    "applied": false
  },
  {
    "id": "proposal_20260601_094402_872296",
    "created_at": "2026-06-01T09:44:02.872326Z",
    "module": "C:\\Users\\tsmit\\realai\\realai\\plugins\\knowledge_rag.py",
    "objective": "Tighten error handling without breaking behavior.",
    "diff": "--- C:\\Users\\tsmit\\realai\\realai\\plugins\\knowledge_rag.py\n+++ C:\\Users\\tsmit\\realai\\realai\\plugins\\knowledge_rag.py (proposed)\n@@ -1,120 +1 @@\n-import json\n-import re\n-from pathlib import Path\n-from typing import Any, Dict, List\n-\n-from realai.plugins.llm_chat import LLMChatPlugin\n-\n-\n-class KnowledgeRAGPlugin:\n-    name = \"knowledge_rag\"\n-\n-    def __init__(self, store_path: str = \"realai_knowledge_store.json\"):\n-        self.store_path = Path(store_path)\n-        self._llm = LLMChatPlugin()\n-        self._docs = self._load_store()\n-\n-    def _load_store(self) -> List[Dict[str, Any]]:\n-        if not self.store_path.exists():\n-            return []\n-        try:\n-            return json.loads(self.store_path.read_text(encoding=\"utf-8\"))\n-        except Exception:\n-            return []\n-\n-    def _save_store(self) -> None:\n-        self.store_path.write_text(json.dumps(self._docs, indent=2), encoding=\"utf-8\")\n-\n-    def ingest_text(self, title: str, content: str, source: str = \"manual\") -> Dict[str, Any]:\n-        doc_id = \"doc_{0}\".format(len(self._docs) + 1)\n-        self._docs.append(\n-            {\n-                \"id\": doc_id,\n-                \"title\": title,\n-                \"content\": content,\n-                \"source\": source,\n-            }\n-        )\n-        self._save_store()\n-        return {\"status\": \"success\", \"id\": doc_id}\n-\n-    def ingest_file(self, file_path: str) -> Dict[str, Any]:\n-        path = Path(file_path)\n-        if not path.exists():\n-            return {\"status\": \"failed\", \"error\": \"File not found: {0}\".format(file_path)}\n-\n-        suffix = path.suffix.lower()\n-        if suffix in (\".md\", \".txt\", \".py\", \".json\", \".toml\", \".yaml\", \".yml\"):\n-            content = path.read_text(encoding=\"utf-8\", errors=\"ignore\")\n-            return self.ingest_text(title=path.name, content=content, source=str(path))\n-\n-        if suffix == \".pdf\":\n-            try:\n-                from pypdf import PdfReader  # type: ignore\n-\n-                reader = PdfReader(str(path))\n-                parts = []\n-                for page in reader.pages:\n-                    parts.append(page.extract_text() or \"\")\n-                content = \"\\n\".join(parts)\n-                return self.ingest_text(title=path.name, content=content, source=str(path))\n-            except Exception as exc:\n-                return {\"status\": \"failed\", \"error\": \"PDF parsing failed: {0}\".format(exc)}\n-\n-        return {\"status\": \"failed\", \"error\": \"Unsupported file type: {0}\".format(suffix)}\n-\n-    def _score(self, query: str, content: str) -> float:\n-        query_terms = set(re.findall(r\"[a-zA-Z0-9_]+\", query.lower()))\n-        content_terms = set(re.findall(r\"[a-zA-Z0-9_]+\", content.lower()))\n-        if not query_terms:\n-            return 0.0\n-        overlap = len(query_terms.intersection(content_terms))\n-        return overlap / float(len(query_terms))\n-\n-    def retrieve(self, query: str, top_k: int = 3) -> List[Dict[str, Any]]:\n-        scored = []\n-        for doc in self._docs:\n-            score = self._score(query, doc.get(\"content\", \"\"))\n-            if score > 0:\n-                scored.append((score, doc))\n-        scored.sort(key=lambda item: item[0], reverse=True)\n-\n-        results = []\n-        for score, doc in scored[:top_k]:\n-            results.append(\n-                {\n-                    \"id\": doc.get(\"id\"),\n-                    \"title\": doc.get(\"title\"),\n-                    \"source\": doc.get(\"source\"),\n-                    \"score\": round(score, 4),\n-                    \"content_preview\": doc.get(\"content\", \"\")[:1000],\n-                }\n-            )\n-        return results\n-\n-    def query(self, question: str, top_k: int = 3) -> Dict[str, Any]:\n-        retrieved = self.retrieve(question, top_k=top_k)\n-        if not retrieved:\n-            return {\n-                \"status\": \"success\",\n-                \"question\": question,\n-                \"answer\": \"No relevant knowledge found in local store.\",\n-                \"sources\": [],\n-            }\n-\n-        context = \"\\n\\n\".join(\n-            \"[{0}] {1}\\n{2}\".format(item[\"id\"], item[\"title\"], item[\"content_preview\"])\n-            for item in retrieved\n-        )\n-        prompt = (\n-            \"Answer the question using only the provided context. Cite source ids in brackets.\\n\"\n-            \"Question: {0}\\n\\nContext:\\n{1}\"\n-        ).format(question, context)\n-        generated = self._llm.generate(prompt=prompt, temperature=0.2, max_tokens=600)\n-        return {\n-            \"status\": \"success\",\n-            \"question\": question,\n-            \"answer\": generated.get(\"content\", \"\"),\n-            \"sources\": retrieved,\n-            \"provider\": generated.get(\"provider\"),\n-        }\n+[RealAI] No LLM backend available. Install ollama or set GROQ_API_KEY / OPENROUTER_API_KEY / XAI_API_KEY.",
    "proposed_content": "[RealAI] No LLM backend available. Install ollama or set GROQ_API_KEY / OPENROUTER_API_KEY / XAI_API_KEY.",
    "applied": false
  },
  {
    "id": "proposal_20260601_095648_042548",
    "created_at": "2026-06-01T09:56:48.042588Z",
    "module": "C:\\Users\\tsmit\\realai\\realai\\plugins\\knowledge_rag.py",
    "objective": "Tighten error handling without breaking behavior.",
    "diff": "--- C:\\Users\\tsmit\\realai\\realai\\plugins\\knowledge_rag.py\n+++ C:\\Users\\tsmit\\realai\\realai\\plugins\\knowledge_rag.py (proposed)\n@@ -1,120 +1 @@\n-import json\n-import re\n-from pathlib import Path\n-from typing import Any, Dict, List\n-\n-from realai.plugins.llm_chat import LLMChatPlugin\n-\n-\n-class KnowledgeRAGPlugin:\n-    name = \"knowledge_rag\"\n-\n-    def __init__(self, store_path: str = \"realai_knowledge_store.json\"):\n-        self.store_path = Path(store_path)\n-        self._llm = LLMChatPlugin()\n-        self._docs = self._load_store()\n-\n-    def _load_store(self) -> List[Dict[str, Any]]:\n-        if not self.store_path.exists():\n-            return []\n-        try:\n-            return json.loads(self.store_path.read_text(encoding=\"utf-8\"))\n-        except Exception:\n-            return []\n-\n-    def _save_store(self) -> None:\n-        self.store_path.write_text(json.dumps(self._docs, indent=2), encoding=\"utf-8\")\n-\n-    def ingest_text(self, title: str, content: str, source: str = \"manual\") -> Dict[str, Any]:\n-        doc_id = \"doc_{0}\".format(len(self._docs) + 1)\n-        self._docs.append(\n-            {\n-                \"id\": doc_id,\n-                \"title\": title,\n-                \"content\": content,\n-                \"source\": source,\n-            }\n-        )\n-        self._save_store()\n-        return {\"status\": \"success\", \"id\": doc_id}\n-\n-    def ingest_file(self, file_path: str) -> Dict[str, Any]:\n-        path = Path(file_path)\n-        if not path.exists():\n-            return {\"status\": \"failed\", \"error\": \"File not found: {0}\".format(file_path)}\n-\n-        suffix = path.suffix.lower()\n-        if suffix in (\".md\", \".txt\", \".py\", \".json\", \".toml\", \".yaml\", \".yml\"):\n-            content = path.read_text(encoding=\"utf-8\", errors=\"ignore\")\n-            return self.ingest_text(title=path.name, content=content, source=str(path))\n-\n-        if suffix == \".pdf\":\n-            try:\n-                from pypdf import PdfReader  # type: ignore\n-\n-                reader = PdfReader(str(path))\n-                parts = []\n-                for page in reader.pages:\n-                    parts.append(page.extract_text() or \"\")\n-                content = \"\\n\".join(parts)\n-                return self.ingest_text(title=path.name, content=content, source=str(path))\n-            except Exception as exc:\n-                return {\"status\": \"failed\", \"error\": \"PDF parsing failed: {0}\".format(exc)}\n-\n-        return {\"status\": \"failed\", \"error\": \"Unsupported file type: {0}\".format(suffix)}\n-\n-    def _score(self, query: str, content: str) -> float:\n-        query_terms = set(re.findall(r\"[a-zA-Z0-9_]+\", query.lower()))\n-        content_terms = set(re.findall(r\"[a-zA-Z0-9_]+\", content.lower()))\n-        if not query_terms:\n-            return 0.0\n-        overlap = len(query_terms.intersection(content_terms))\n-        return overlap / float(len(query_terms))\n-\n-    def retrieve(self, query: str, top_k: int = 3) -> List[Dict[str, Any]]:\n-        scored = []\n-        for doc in self._docs:\n-            score = self._score(query, doc.get(\"content\", \"\"))\n-            if score > 0:\n-                scored.append((score, doc))\n-        scored.sort(key=lambda item: item[0], reverse=True)\n-\n-        results = []\n-        for score, doc in scored[:top_k]:\n-            results.append(\n-                {\n-                    \"id\": doc.get(\"id\"),\n-                    \"title\": doc.get(\"title\"),\n-                    \"source\": doc.get(\"source\"),\n-                    \"score\": round(score, 4),\n-                    \"content_preview\": doc.get(\"content\", \"\")[:1000],\n-                }\n-            )\n-        return results\n-\n-    def query(self, question: str, top_k: int = 3) -> Dict[str, Any]:\n-        retrieved = self.retrieve(question, top_k=top_k)\n-        if not retrieved:\n-            return {\n-                \"status\": \"success\",\n-                \"question\": question,\n-                \"answer\": \"No relevant knowledge found in local store.\",\n-                \"sources\": [],\n-            }\n-\n-        context = \"\\n\\n\".join(\n-            \"[{0}] {1}\\n{2}\".format(item[\"id\"], item[\"title\"], item[\"content_preview\"])\n-            for item in retrieved\n-        )\n-        prompt = (\n-            \"Answer the question using only the provided context. Cite source ids in brackets.\\n\"\n-            \"Question: {0}\\n\\nContext:\\n{1}\"\n-        ).format(question, context)\n-        generated = self._llm.generate(prompt=prompt, temperature=0.2, max_tokens=600)\n-        return {\n-            \"status\": \"success\",\n-            \"question\": question,\n-            \"answer\": generated.get(\"content\", \"\"),\n-            \"sources\": retrieved,\n-            \"provider\": generated.get(\"provider\"),\n-        }\n+[RealAI] No LLM backend available. Install ollama or set GROQ_API_KEY / OPENROUTER_API_KEY / XAI_API_KEY.",
    "proposed_content": "[RealAI] No LLM backend available. Install ollama or set GROQ_API_KEY / OPENROUTER_API_KEY / XAI_API_KEY.",
    "applied": false
  },
  {
    "id": "proposal_20260601_100734_054828",
    "created_at": "2026-06-01T10:07:34.054861Z",
    "module": "C:\\Users\\tsmit\\realai\\realai\\plugins\\knowledge_rag.py",
    "objective": "Tighten error handling without breaking behavior.",
    "diff": "--- C:\\Users\\tsmit\\realai\\realai\\plugins\\knowledge_rag.py\n+++ C:\\Users\\tsmit\\realai\\realai\\plugins\\knowledge_rag.py (proposed)\n@@ -1,120 +1 @@\n-import json\n-import re\n-from pathlib import Path\n-from typing import Any, Dict, List\n-\n-from realai.plugins.llm_chat import LLMChatPlugin\n-\n-\n-class KnowledgeRAGPlugin:\n-    name = \"knowledge_rag\"\n-\n-    def __init__(self, store_path: str = \"realai_knowledge_store.json\"):\n-        self.store_path = Path(store_path)\n-        self._llm = LLMChatPlugin()\n-        self._docs = self._load_store()\n-\n-    def _load_store(self) -> List[Dict[str, Any]]:\n-        if not self.store_path.exists():\n-            return []\n-        try:\n-            return json.loads(self.store_path.read_text(encoding=\"utf-8\"))\n-        except Exception:\n-            return []\n-\n-    def _save_store(self) -> None:\n-        self.store_path.write_text(json.dumps(self._docs, indent=2), encoding=\"utf-8\")\n-\n-    def ingest_text(self, title: str, content: str, source: str = \"manual\") -> Dict[str, Any]:\n-        doc_id = \"doc_{0}\".format(len(self._docs) + 1)\n-        self._docs.append(\n-            {\n-                \"id\": doc_id,\n-                \"title\": title,\n-                \"content\": content,\n-                \"source\": source,\n-            }\n-        )\n-        self._save_store()\n-        return {\"status\": \"success\", \"id\": doc_id}\n-\n-    def ingest_file(self, file_path: str) -> Dict[str, Any]:\n-        path = Path(file_path)\n-        if not path.exists():\n-            return {\"status\": \"failed\", \"error\": \"File not found: {0}\".format(file_path)}\n-\n-        suffix = path.suffix.lower()\n-        if suffix in (\".md\", \".txt\", \".py\", \".json\", \".toml\", \".yaml\", \".yml\"):\n-            content = path.read_text(encoding=\"utf-8\", errors=\"ignore\")\n-            return self.ingest_text(title=path.name, content=content, source=str(path))\n-\n-        if suffix == \".pdf\":\n-            try:\n-                from pypdf import PdfReader  # type: ignore\n-\n-                reader = PdfReader(str(path))\n-                parts = []\n-                for page in reader.pages:\n-                    parts.append(page.extract_text() or \"\")\n-                content = \"\\n\".join(parts)\n-                return self.ingest_text(title=path.name, content=content, source=str(path))\n-            except Exception as exc:\n-                return {\"status\": \"failed\", \"error\": \"PDF parsing failed: {0}\".format(exc)}\n-\n-        return {\"status\": \"failed\", \"error\": \"Unsupported file type: {0}\".format(suffix)}\n-\n-    def _score(self, query: str, content: str) -> float:\n-        query_terms = set(re.findall(r\"[a-zA-Z0-9_]+\", query.lower()))\n-        content_terms = set(re.findall(r\"[a-zA-Z0-9_]+\", content.lower()))\n-        if not query_terms:\n-            return 0.0\n-        overlap = len(query_terms.intersection(content_terms))\n-        return overlap / float(len(query_terms))\n-\n-    def retrieve(self, query: str, top_k: int = 3) -> List[Dict[str, Any]]:\n-        scored = []\n-        for doc in self._docs:\n-            score = self._score(query, doc.get(\"content\", \"\"))\n-            if score > 0:\n-                scored.append((score, doc))\n-        scored.sort(key=lambda item: item[0], reverse=True)\n-\n-        results = []\n-        for score, doc in scored[:top_k]:\n-            results.append(\n-                {\n-                    \"id\": doc.get(\"id\"),\n-                    \"title\": doc.get(\"title\"),\n-                    \"source\": doc.get(\"source\"),\n-                    \"score\": round(score, 4),\n-                    \"content_preview\": doc.get(\"content\", \"\")[:1000],\n-                }\n-            )\n-        return results\n-\n-    def query(self, question: str, top_k: int = 3) -> Dict[str, Any]:\n-        retrieved = self.retrieve(question, top_k=top_k)\n-        if not retrieved:\n-            return {\n-                \"status\": \"success\",\n-                \"question\": question,\n-                \"answer\": \"No relevant knowledge found in local store.\",\n-                \"sources\": [],\n-            }\n-\n-        context = \"\\n\\n\".join(\n-            \"[{0}] {1}\\n{2}\".format(item[\"id\"], item[\"title\"], item[\"content_preview\"])\n-            for item in retrieved\n-        )\n-        prompt = (\n-            \"Answer the question using only the provided context. Cite source ids in brackets.\\n\"\n-            \"Question: {0}\\n\\nContext:\\n{1}\"\n-        ).format(question, context)\n-        generated = self._llm.generate(prompt=prompt, temperature=0.2, max_tokens=600)\n-        return {\n-            \"status\": \"success\",\n-            \"question\": question,\n-            \"answer\": generated.get(\"content\", \"\"),\n-            \"sources\": retrieved,\n-            \"provider\": generated.get(\"provider\"),\n-        }\n+[RealAI] No LLM backend available. Install ollama or set GROQ_API_KEY / OPENROUTER_API_KEY / XAI_API_KEY.",
    "proposed_content": "[RealAI] No LLM backend available. Install ollama or set GROQ_API_KEY / OPENROUTER_API_KEY / XAI_API_KEY.",
    "applied": false
  },
  {
    "id": "proposal_20260601_101714_251969",
    "created_at": "2026-06-01T10:17:14.251995Z",
    "module": "C:\\Users\\tsmit\\realai\\realai\\plugins\\knowledge_rag.py",
    "objective": "Tighten error handling without breaking behavior.",
    "diff": "--- C:\\Users\\tsmit\\realai\\realai\\plugins\\knowledge_rag.py\n+++ C:\\Users\\tsmit\\realai\\realai\\plugins\\knowledge_rag.py (proposed)\n@@ -1,120 +1 @@\n-import json\n-import re\n-from pathlib import Path\n-from typing import Any, Dict, List\n-\n-from realai.plugins.llm_chat import LLMChatPlugin\n-\n-\n-class KnowledgeRAGPlugin:\n-    name = \"knowledge_rag\"\n-\n-    def __init__(self, store_path: str = \"realai_knowledge_store.json\"):\n-        self.store_path = Path(store_path)\n-        self._llm = LLMChatPlugin()\n-        self._docs = self._load_store()\n-\n-    def _load_store(self) -> List[Dict[str, Any]]:\n-        if not self.store_path.exists():\n-            return []\n-        try:\n-            return json.loads(self.store_path.read_text(encoding=\"utf-8\"))\n-        except Exception:\n-            return []\n-\n-    def _save_store(self) -> None:\n-        self.store_path.write_text(json.dumps(self._docs, indent=2), encoding=\"utf-8\")\n-\n-    def ingest_text(self, title: str, content: str, source: str = \"manual\") -> Dict[str, Any]:\n-        doc_id = \"doc_{0}\".format(len(self._docs) + 1)\n-        self._docs.append(\n-            {\n-                \"id\": doc_id,\n-                \"title\": title,\n-                \"content\": content,\n-                \"source\": source,\n-            }\n-        )\n-        self._save_store()\n-        return {\"status\": \"success\", \"id\": doc_id}\n-\n-    def ingest_file(self, file_path: str) -> Dict[str, Any]:\n-        path = Path(file_path)\n-        if not path.exists():\n-            return {\"status\": \"failed\", \"error\": \"File not found: {0}\".format(file_path)}\n-\n-        suffix = path.suffix.lower()\n-        if suffix in (\".md\", \".txt\", \".py\", \".json\", \".toml\", \".yaml\", \".yml\"):\n-            content = path.read_text(encoding=\"utf-8\", errors=\"ignore\")\n-            return self.ingest_text(title=path.name, content=content, source=str(path))\n-\n-        if suffix == \".pdf\":\n-            try:\n-                from pypdf import PdfReader  # type: ignore\n-\n-                reader = PdfReader(str(path))\n-                parts = []\n-                for page in reader.pages:\n-                    parts.append(page.extract_text() or \"\")\n-                content = \"\\n\".join(parts)\n-                return self.ingest_text(title=path.name, content=content, source=str(path))\n-            except Exception as exc:\n-                return {\"status\": \"failed\", \"error\": \"PDF parsing failed: {0}\".format(exc)}\n-\n-        return {\"status\": \"failed\", \"error\": \"Unsupported file type: {0}\".format(suffix)}\n-\n-    def _score(self, query: str, content: str) -> float:\n-        query_terms = set(re.findall(r\"[a-zA-Z0-9_]+\", query.lower()))\n-        content_terms = set(re.findall(r\"[a-zA-Z0-9_]+\", content.lower()))\n-        if not query_terms:\n-            return 0.0\n-        overlap = len(query_terms.intersection(content_terms))\n-        return overlap / float(len(query_terms))\n-\n-    def retrieve(self, query: str, top_k: int = 3) -> List[Dict[str, Any]]:\n-        scored = []\n-        for doc in self._docs:\n-            score = self._score(query, doc.get(\"content\", \"\"))\n-            if score > 0:\n-                scored.append((score, doc))\n-        scored.sort(key=lambda item: item[0], reverse=True)\n-\n-        results = []\n-        for score, doc in scored[:top_k]:\n-            results.append(\n-                {\n-                    \"id\": doc.get(\"id\"),\n-                    \"title\": doc.get(\"title\"),\n-                    \"source\": doc.get(\"source\"),\n-                    \"score\": round(score, 4),\n-                    \"content_preview\": doc.get(\"content\", \"\")[:1000],\n-                }\n-            )\n-        return results\n-\n-    def query(self, question: str, top_k: int = 3) -> Dict[str, Any]:\n-        retrieved = self.retrieve(question, top_k=top_k)\n-        if not retrieved:\n-            return {\n-                \"status\": \"success\",\n-                \"question\": question,\n-                \"answer\": \"No relevant knowledge found in local store.\",\n-                \"sources\": [],\n-            }\n-\n-        context = \"\\n\\n\".join(\n-            \"[{0}] {1}\\n{2}\".format(item[\"id\"], item[\"title\"], item[\"content_preview\"])\n-            for item in retrieved\n-        )\n-        prompt = (\n-            \"Answer the question using only the provided context. Cite source ids in brackets.\\n\"\n-            \"Question: {0}\\n\\nContext:\\n{1}\"\n-        ).format(question, context)\n-        generated = self._llm.generate(prompt=prompt, temperature=0.2, max_tokens=600)\n-        return {\n-            \"status\": \"success\",\n-            \"question\": question,\n-            \"answer\": generated.get(\"content\", \"\"),\n-            \"sources\": retrieved,\n-            \"provider\": generated.get(\"provider\"),\n-        }\n+[RealAI] No LLM backend available. Install ollama or set GROQ_API_KEY / OPENROUTER_API_KEY / XAI_API_KEY.",
    "proposed_content": "[RealAI] No LLM backend available. Install ollama or set GROQ_API_KEY / OPENROUTER_API_KEY / XAI_API_KEY.",
    "applied": false
  },
  {
    "id": "proposal_20260601_102545_098309",
    "created_at": "2026-06-01T10:25:45.098335Z",
    "module": "C:\\Users\\tsmit\\realai\\realai\\plugins\\knowledge_rag.py",
    "objective": "Tighten error handling without breaking behavior.",
    "diff": "--- C:\\Users\\tsmit\\realai\\realai\\plugins\\knowledge_rag.py\n+++ C:\\Users\\tsmit\\realai\\realai\\plugins\\knowledge_rag.py (proposed)\n@@ -1,120 +1 @@\n-import json\n-import re\n-from pathlib import Path\n-from typing import Any, Dict, List\n-\n-from realai.plugins.llm_chat import LLMChatPlugin\n-\n-\n-class KnowledgeRAGPlugin:\n-    name = \"knowledge_rag\"\n-\n-    def __init__(self, store_path: str = \"realai_knowledge_store.json\"):\n-        self.store_path = Path(store_path)\n-        self._llm = LLMChatPlugin()\n-        self._docs = self._load_store()\n-\n-    def _load_store(self) -> List[Dict[str, Any]]:\n-        if not self.store_path.exists():\n-            return []\n-        try:\n-            return json.loads(self.store_path.read_text(encoding=\"utf-8\"))\n-        except Exception:\n-            return []\n-\n-    def _save_store(self) -> None:\n-        self.store_path.write_text(json.dumps(self._docs, indent=2), encoding=\"utf-8\")\n-\n-    def ingest_text(self, title: str, content: str, source: str = \"manual\") -> Dict[str, Any]:\n-        doc_id = \"doc_{0}\".format(len(self._docs) + 1)\n-        self._docs.append(\n-            {\n-                \"id\": doc_id,\n-                \"title\": title,\n-                \"content\": content,\n-                \"source\": source,\n-            }\n-        )\n-        self._save_store()\n-        return {\"status\": \"success\", \"id\": doc_id}\n-\n-    def ingest_file(self, file_path: str) -> Dict[str, Any]:\n-        path = Path(file_path)\n-        if not path.exists():\n-            return {\"status\": \"failed\", \"error\": \"File not found: {0}\".format(file_path)}\n-\n-        suffix = path.suffix.lower()\n-        if suffix in (\".md\", \".txt\", \".py\", \".json\", \".toml\", \".yaml\", \".yml\"):\n-            content = path.read_text(encoding=\"utf-8\", errors=\"ignore\")\n-            return self.ingest_text(title=path.name, content=content, source=str(path))\n-\n-        if suffix == \".pdf\":\n-            try:\n-                from pypdf import PdfReader  # type: ignore\n-\n-                reader = PdfReader(str(path))\n-                parts = []\n-                for page in reader.pages:\n-                    parts.append(page.extract_text() or \"\")\n-                content = \"\\n\".join(parts)\n-                return self.ingest_text(title=path.name, content=content, source=str(path))\n-            except Exception as exc:\n-                return {\"status\": \"failed\", \"error\": \"PDF parsing failed: {0}\".format(exc)}\n-\n-        return {\"status\": \"failed\", \"error\": \"Unsupported file type: {0}\".format(suffix)}\n-\n-    def _score(self, query: str, content: str) -> float:\n-        query_terms = set(re.findall(r\"[a-zA-Z0-9_]+\", query.lower()))\n-        content_terms = set(re.findall(r\"[a-zA-Z0-9_]+\", content.lower()))\n-        if not query_terms:\n-            return 0.0\n-        overlap = len(query_terms.intersection(content_terms))\n-        return overlap / float(len(query_terms))\n-\n-    def retrieve(self, query: str, top_k: int = 3) -> List[Dict[str, Any]]:\n-        scored = []\n-        for doc in self._docs:\n-            score = self._score(query, doc.get(\"content\", \"\"))\n-            if score > 0:\n-                scored.append((score, doc))\n-        scored.sort(key=lambda item: item[0], reverse=True)\n-\n-        results = []\n-        for score, doc in scored[:top_k]:\n-            results.append(\n-                {\n-                    \"id\": doc.get(\"id\"),\n-                    \"title\": doc.get(\"title\"),\n-                    \"source\": doc.get(\"source\"),\n-                    \"score\": round(score, 4),\n-                    \"content_preview\": doc.get(\"content\", \"\")[:1000],\n-                }\n-            )\n-        return results\n-\n-    def query(self, question: str, top_k: int = 3) -> Dict[str, Any]:\n-        retrieved = self.retrieve(question, top_k=top_k)\n-        if not retrieved:\n-            return {\n-                \"status\": \"success\",\n-                \"question\": question,\n-                \"answer\": \"No relevant knowledge found in local store.\",\n-                \"sources\": [],\n-            }\n-\n-        context = \"\\n\\n\".join(\n-            \"[{0}] {1}\\n{2}\".format(item[\"id\"], item[\"title\"], item[\"content_preview\"])\n-            for item in retrieved\n-        )\n-        prompt = (\n-            \"Answer the question using only the provided context. Cite source ids in brackets.\\n\"\n-            \"Question: {0}\\n\\nContext:\\n{1}\"\n-        ).format(question, context)\n-        generated = self._llm.generate(prompt=prompt, temperature=0.2, max_tokens=600)\n-        return {\n-            \"status\": \"success\",\n-            \"question\": question,\n-            \"answer\": generated.get(\"content\", \"\"),\n-            \"sources\": retrieved,\n-            \"provider\": generated.get(\"provider\"),\n-        }\n+[RealAI] No LLM backend available. Install ollama or set GROQ_API_KEY / OPENROUTER_API_KEY / XAI_API_KEY.",
    "proposed_content": "[RealAI] No LLM backend available. Install ollama or set GROQ_API_KEY / OPENROUTER_API_KEY / XAI_API_KEY.",
    "applied": false
  },
  {
    "id": "proposal_20260601_104605_386096",
    "created_at": "2026-06-01T10:46:05.386132Z",
    "module": "C:\\Users\\tsmit\\realai\\realai\\plugins\\knowledge_rag.py",
    "objective": "Tighten error handling without breaking behavior.",
    "diff": "--- C:\\Users\\tsmit\\realai\\realai\\plugins\\knowledge_rag.py\n+++ C:\\Users\\tsmit\\realai\\realai\\plugins\\knowledge_rag.py (proposed)\n@@ -1,120 +1 @@\n-import json\n-import re\n-from pathlib import Path\n-from typing import Any, Dict, List\n-\n-from realai.plugins.llm_chat import LLMChatPlugin\n-\n-\n-class KnowledgeRAGPlugin:\n-    name = \"knowledge_rag\"\n-\n-    def __init__(self, store_path: str = \"realai_knowledge_store.json\"):\n-        self.store_path = Path(store_path)\n-        self._llm = LLMChatPlugin()\n-        self._docs = self._load_store()\n-\n-    def _load_store(self) -> List[Dict[str, Any]]:\n-        if not self.store_path.exists():\n-            return []\n-        try:\n-            return json.loads(self.store_path.read_text(encoding=\"utf-8\"))\n-        except Exception:\n-            return []\n-\n-    def _save_store(self) -> None:\n-        self.store_path.write_text(json.dumps(self._docs, indent=2), encoding=\"utf-8\")\n-\n-    def ingest_text(self, title: str, content: str, source: str = \"manual\") -> Dict[str, Any]:\n-        doc_id = \"doc_{0}\".format(len(self._docs) + 1)\n-        self._docs.append(\n-            {\n-                \"id\": doc_id,\n-                \"title\": title,\n-                \"content\": content,\n-                \"source\": source,\n-            }\n-        )\n-        self._save_store()\n-        return {\"status\": \"success\", \"id\": doc_id}\n-\n-    def ingest_file(self, file_path: str) -> Dict[str, Any]:\n-        path = Path(file_path)\n-        if not path.exists():\n-            return {\"status\": \"failed\", \"error\": \"File not found: {0}\".format(file_path)}\n-\n-        suffix = path.suffix.lower()\n-        if suffix in (\".md\", \".txt\", \".py\", \".json\", \".toml\", \".yaml\", \".yml\"):\n-            content = path.read_text(encoding=\"utf-8\", errors=\"ignore\")\n-            return self.ingest_text(title=path.name, content=content, source=str(path))\n-\n-        if suffix == \".pdf\":\n-            try:\n-                from pypdf import PdfReader  # type: ignore\n-\n-                reader = PdfReader(str(path))\n-                parts = []\n-                for page in reader.pages:\n-                    parts.append(page.extract_text() or \"\")\n-                content = \"\\n\".join(parts)\n-                return self.ingest_text(title=path.name, content=content, source=str(path))\n-            except Exception as exc:\n-                return {\"status\": \"failed\", \"error\": \"PDF parsing failed: {0}\".format(exc)}\n-\n-        return {\"status\": \"failed\", \"error\": \"Unsupported file type: {0}\".format(suffix)}\n-\n-    def _score(self, query: str, content: str) -> float:\n-        query_terms = set(re.findall(r\"[a-zA-Z0-9_]+\", query.lower()))\n-        content_terms = set(re.findall(r\"[a-zA-Z0-9_]+\", content.lower()))\n-        if not query_terms:\n-            return 0.0\n-        overlap = len(query_terms.intersection(content_terms))\n-        return overlap / float(len(query_terms))\n-\n-    def retrieve(self, query: str, top_k: int = 3) -> List[Dict[str, Any]]:\n-        scored = []\n-        for doc in self._docs:\n-            score = self._score(query, doc.get(\"content\", \"\"))\n-            if score > 0:\n-                scored.append((score, doc))\n-        scored.sort(key=lambda item: item[0], reverse=True)\n-\n-        results = []\n-        for score, doc in scored[:top_k]:\n-            results.append(\n-                {\n-                    \"id\": doc.get(\"id\"),\n-                    \"title\": doc.get(\"title\"),\n-                    \"source\": doc.get(\"source\"),\n-                    \"score\": round(score, 4),\n-                    \"content_preview\": doc.get(\"content\", \"\")[:1000],\n-                }\n-            )\n-        return results\n-\n-    def query(self, question: str, top_k: int = 3) -> Dict[str, Any]:\n-        retrieved = self.retrieve(question, top_k=top_k)\n-        if not retrieved:\n-            return {\n-                \"status\": \"success\",\n-                \"question\": question,\n-                \"answer\": \"No relevant knowledge found in local store.\",\n-                \"sources\": [],\n-            }\n-\n-        context = \"\\n\\n\".join(\n-            \"[{0}] {1}\\n{2}\".format(item[\"id\"], item[\"title\"], item[\"content_preview\"])\n-            for item in retrieved\n-        )\n-        prompt = (\n-            \"Answer the question using only the provided context. Cite source ids in brackets.\\n\"\n-            \"Question: {0}\\n\\nContext:\\n{1}\"\n-        ).format(question, context)\n-        generated = self._llm.generate(prompt=prompt, temperature=0.2, max_tokens=600)\n-        return {\n-            \"status\": \"success\",\n-            \"question\": question,\n-            \"answer\": generated.get(\"content\", \"\"),\n-            \"sources\": retrieved,\n-            \"provider\": generated.get(\"provider\"),\n-        }\n+[RealAI] No LLM backend available. Install ollama or set GROQ_API_KEY / OPENROUTER_API_KEY / XAI_API_KEY.",
    "proposed_content": "[RealAI] No LLM backend available. Install ollama or set GROQ_API_KEY / OPENROUTER_API_KEY / XAI_API_KEY.",
    "applied": false
  }
]